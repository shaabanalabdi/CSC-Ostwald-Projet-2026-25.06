// ============================================================
// HeroSlide.js — Entité d'une slide du carrousel Hero (page d'accueil).
// Adossée à la table `hero_slide` (database.sql §13). Corps vide —
// Entity hydrate l'instance depuis une ligne SQL.
// ============================================================

import { Entity } from '../core/Entity.js';

export class HeroSlide extends Entity {}
